name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build Release
        run: |
          xcodebuild -project PDFStitch.xcodeproj \
            -scheme PDFStitch \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="-" \
            build

      - name: Create DMG
        run: |
          brew install create-dmg
          
          APP_DIR="build/Build/Products/Release"
          
          # create-dmg may return exit code 2 if it can't set background, but DMG is still valid
          create-dmg \
            --volname "PDF Stitch" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "PDF Stitch.app" 150 190 \
            --hide-extension "PDF Stitch.app" \
            --app-drop-link 450 190 \
            "PDFStitch-${GITHUB_REF_NAME}.dmg" \
            "$APP_DIR/PDF Stitch.app" || true
          
          # Verify DMG was created
          ls -la PDFStitch-*.dmg

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          files: PDFStitch-*.dmg
          generate_release_notes: true
          draft: false
